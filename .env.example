# Data Foundry (Postgres + pgvector). Start the DB with `docker compose up -d`, then migrate:
#   bun run Scripts/FoundryMigrate.ts && bun run Scripts/FoundrySmoke.ts
DATABASE_URL=postgres://postgres:postgres@localhost:5432/shahd
